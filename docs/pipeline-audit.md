# Asset and agent pipeline audit

2026-09-08. Source audit of the working tree before the current optimization pass. Findings below describe that baseline; an assigned fix is not a verified result. This review read the repository architecture, asset, format, storage, service, input, testing and evidence documents alongside the native and backend implementation. Apple and OpenAI references were checked for the relevant APIs; this is not a claim to have read every Apple document.

The existing separation is sound: **Blender/USD contains authored geometry, semantic JSON identifies editable assemblies, Astra proposes changes, and RealityKit renders the accepted result locally.** The main missing capability is choosing and loading a useful level of detail while preserving the identity of the assembly already being discussed. A new serialization format or rendering engine does not solve that by itself.

## Findings that affect the demo

| Priority | Finding and code evidence | Smallest useful correction and acceptance test |
| --- | --- | --- |
| P1 | **Interior deconstruction is unavailable in the imported exterior.** The importer builds one root plus one node per approved descriptor part; the catalog exposes frame + 18 joined servers. `Rendering/SceneRenderer.swift:129–144`, `examples/imported-rack/source-catalog.json`. An instruction cannot recover absent component boundaries. | Export a small set of teaching assemblies from the actual `.blend` detail library. Advertise available detail separately from loaded detail. “Pull out all servers” must move the 18 server wrappers with zero interior downloads; “explain this server” may load that server's systems without expanding every object. |
| P1 | **The existing asset-load entry point replaces the scene.** `SceneController.swift:445–506` creates a new live scene and resets selection; `SceneRenderer.swift:143` creates a new document. Calling it for detail expansion would discard the rack context and previous edit history. | Keep this API for initial scene replacement. For refinement, prepare approved resources separately and commit a bounded subtree edit under the existing server identity. Verify that rack/scene IDs, parent placement, unrelated nodes and prior transforms survive; a stale completion must not install. |
| P1 | **Astra's baseline context is a full current snapshot, not conversation history or an assembly index.** `services/session/src/astra/client.ts:99–102` serializes everything; `session.ts:129` supplies only the current request/selection/snapshot. Realtime's conversation is a separate provider session. The native node model has no rest pose, detail availability or bounds (`SceneModels.swift:222–257`). | Supply compact assembly records, selected ancestry, relevant siblings, current transforms and a bounded history of accepted outcomes. Keep immutable asset digests/triangle data outside repeated node prose. Test “those units” after an accepted group edit with no new tap; the author must resolve the previously discussed group or ask when ambiguous. |
| P1 | **Narration can overstate execution.** The newly observed `Conversation/RealtimeWire.swift:116` calls an installation “visible”; failed/cancelled instructions at `:119–122` imply no edit applied. `SceneController.swift:758–765` confirms installation, not an on-screen frame. A disconnect after commit may leave a changed scene. | Say “installed in the scene” from an installation receipt. Distinguish confirmed rejection, known installed change, and unconfirmed outcome. Test hidden/off-screen installation and loss of the reply after local commit; narration must not invent visibility or rollback. |
| P2 | **The initial resident cache cannot support successive detail packages.** `Rendering/ImportedAsset.swift:166–188` permanently retains prepared prototypes and rejects a third distinct digest. `SceneState.swift:12–13,158` retains request records and whole-document undo history without a bound. | Use a small measured prototype budget, a bounded staging slot, and explicit active/undo/pending resource pins. Coalesce repeated loads. Exercise three approved packages and a return to earlier content; eviction must neither reject ordinary navigation indefinitely nor break Undo. The native performance agent owns this correction. |
| P2 | **Automatic pointing exposes avoidable repeated rendering work.** `SceneController.swift:557–559` updates renderer selection on each observation. Baseline `SceneRenderer.swift:59–66,303–326` does not skip an unchanged target and rewrites materials for every procedural node; `:279–299` eagerly creates 12 outline meshes per imported server. | Skip unchanged selections, update only previous/new highlights, and allocate one reusable outline on demand. Verify selection does not re-create imported mesh resources or traverse/rewrite every procedural model for an unchanged hover. |
| P2 | **Saving semantic JSON is not yet saving a self-contained imported project.** `Storage/SceneDocumentStore.swift:105–163,236–284` stores and validates JSON references, but not the approved descriptors or source binaries. `ImportedAsset.swift:103–106` keeps downloaded bytes in Caches; fresh `SceneRenderer.prepareScene` requires an already prepared catalog (`:159–165`). | Before adding Save/Open UI, retain a versioned asset manifest and required source bytes or a verified reacquisition source, prepare those assets, then install the saved scene. Test a fresh process and purged cache. This is a persistence boundary, not a prerequisite for the current transient demo. |

Native paths above are relative to `packages/SpatialKit/Sources/SpatialApple`; `SceneState.swift` and `SceneModels.swift` are in `SpatialCore`. Line anchors identify the audited baseline and may move during the current changes.

## Representation and tool decisions

Keep two linked data sets, rather than converting all CAD triangles into model text:

1. **Authored artifact:** immutable USDZ/Reality files plus source/export hashes, units, bounds, material information and validated part bindings. Blender and OpenUSD perform preparation before the interaction. The app downloads or loads a known package; the model never supplies a file path or arbitrary asset URL.
2. **Accepted semantic scene:** stable instance IDs, containment, current transforms, named roles, provenance and references into those artifacts. Add a small assembly catalog with rest poses and available detail levels. Send Astra the useful semantic view; keep the complete source-object index available for targeted inspection, outside the normal prompt.

Semantic granularity and visual LOD are different decisions. Astra chooses whether the explanation needs a rack, whole servers, cooling/memory/compute systems, or one part. The runtime chooses an affordable visual variant for that same semantic identity. Changing a visual variant must not change which server “this one” refers to. There is no need to instantiate or describe every screw to explain airflow.

The model-facing vocabulary should express bounded intent using observed IDs: translation of an explicit set, arrangement at an explicit assembly level, or requesting a known detail level. The normalizer performs arithmetic and expands the chosen set into ordinary absolute scene edits. Keep the strict reducer/hash/epoch contract; do not make Astra repeat quaternions and opaque hashes merely to move several servers. Destructive or recursive depth should be explicit. A request for all rack units must not implicitly mean every descendant.

The live path remains one Realtime conversation and an Astra scene author. OpenAI documents the function-call arguments → application execution → matching `function_call_output` → final response sequence. That sequence gives the application a place to provide accepted scene results; it does not automatically share Realtime conversation history with a separate Responses request. [Realtime function calling](https://developers.openai.com/api/docs/guides/realtime-conversations#function-calling).

## Asset performance decisions supported by sources

Preserve programmatic access at teaching-assembly boundaries and merge decoration inside them. Apple documents hierarchy-preserving `Entity` loading and flattened model loading; the latter sacrifices descendant access. Its performance guidance recommends shared resources, simpler collision shapes, asynchronous loading and smaller asset packages. These support the current native route; they do not establish that a different SDK would be faster. [Entity loading](https://developer.apple.com/documentation/realitykit/loading-entities-from-a-file), [CPU and asset performance](https://developer.apple.com/documentation/realitykit/reducing-cpu-utilization-in-your-realitykit-app).

OpenUSD supports deferred payload composition and an efficient model hierarchy for discovering important assemblies. That is useful during processing, but a USD payload declaration does not prove that RealityKit exposes the same runtime load/unload controls. Use explicit approved native packages until the selected API demonstrates selective loading. [OpenUSD load/unload and model hierarchy](https://openusd.org/release/glossary.html#usdglossary-loadunload).

The recorded native samples are **macOS preparation/installation measurements**, not iPhone FPS: raw source 25.79 MB / 2.99 million expanded triangles, with 4.126 seconds preparation in one sample. The conservative derivative is 10.55 MB and the distance derivative 9.33 MB. Source identity, hierarchy and processing checks are recorded in [native import evidence](../evidence/imported-rack-native.json) and [mobile derivative validation](../evidence/mobile-rack-validation.json). Measure physical iPhone and iPad peak footprint, first visible frame, insertion hitch, draw calls and steady frame time before calling any profile optimized for those devices. See the separate [Apple import API reference](realitykit-import-references.md).

## Documentation and code readability

Keep `SpatialCore` as the accepted-data boundary and `SpatialApple` as its native projection. Keep example-specific names, part bindings and teaching metadata in `examples/imported-rack`; keep tool aliases and compact context in `services/session/src/astra`. An asset-detail coordinator should orchestrate existing preparation and scene edits, not introduce another scene store or reducer. A public SDK package is unnecessary to prove these boundaries; the existing second generated example is the useful generality test.

The baseline docs contain material drift to correct during this pass:

- `RESEARCH.md:114–116` still calls imported hierarchy/native round trips unverified and says the iPhone has not run the app.
- `services/session/README.md` describes transcript forwarding and excludes voice tool calls, despite the unified Realtime tool path.
- `docs/asset-pipeline.md:44,68,106` and `docs/data-formats.md:24` describe array expansion as implemented; the baseline authoring union has no array operation.
- Several documents promise a future `contracts/schema/` file although `contracts/scene.schema.json` already exists. The contract README initially lists six geometry kinds before later documenting `importedAsset`.
- `docs/gestures.md:17,36` describes explicit Hand mode, conflicting with the requested automatic tracking change.
- `evidence/README.md:24` reports a precise 10.066-second app import without a tracked timing receipt. Preserve the screenshot as appearance evidence and add the sanitized measured event if retaining the timing claim.

One acceptance sequence should drive the next work: load the new rack → pull out whole servers → ask about the previously discussed server → load only its useful systems → add an illustrative explanation marker → interrupt a second detail load → return to the overview. Record the accepted IDs, selected depth, bytes fetched, native resources retained, time to useful visual and truthful final explanation at every step. This establishes the framework's behavior more convincingly than adding more tool names.
